# KitchenMate 小程序（wechat_app）

家庭点餐小程序前端，对接项目 `backend` 提供的 API。

## 开发

1. 用微信开发者工具打开本目录（`wechat_app`）
2. 配置 `config.js` 中的 `apiBaseUrl`
3. 安装依赖后构建 npm：`npm install`，再在开发者工具中选择「工具 → 构建 npm」

## 主要页面

- 首页 / 点餐 / 订单 / 我的（Tab）
- 家庭、聚会、菜品管理、反馈、关于
